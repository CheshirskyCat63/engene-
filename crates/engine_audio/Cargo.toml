[package]
name = "engine_audio"
version = "0.1.0"
edition = "2021"

[dependencies]
engine_core = { path = "../engine_core" }
glam = "0.30"
serde = { version = "1.0", features = ["derive"] }
wgpu = "0.19"
