[package]
name = "engine_content"
version = "0.1.0"
edition = "2021"

[dependencies]
engine_core = { path = "../engine_core" }
serde = { version = "1", features = ["derive"] }
walkdir = "2.4"
ron = "0.8"

[dev-dependencies]
tempfile = "3"
