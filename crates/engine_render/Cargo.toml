[package]
name = "engine_render"
version = "0.1.0"
edition = "2021"

[dependencies]
engine_core = { path = "../engine_core" }
glam = "0.30"
wgpu = "0.19"
bytemuck = { version = "1.0", features = ["derive"] }
rand = "0.8"
tracing = "0.1"
serde = { version = "1.0", features = ["derive"] }
winit = "0.29"
